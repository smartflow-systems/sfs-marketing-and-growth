name: CI Smoke
on:
  push:
    branches: [ main, tune/** ]
  workflow_dispatch: {}

# Security: Explicitly set minimal permissions
permissions: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from Actions!"
