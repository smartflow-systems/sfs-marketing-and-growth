name: SFS CI Deploy
permissions:
  contents: read
on: { push: { branches: [ main ] } }
jobs:
  deploy:
    uses: smartflow-systems/SmartFlowSite/.github/workflows/sfs-ci-deploy.yml@main
    secrets: inherit
