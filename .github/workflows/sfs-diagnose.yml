name: sfs-diagnose
on:
  pull_request: { branches: [ main ] }
  push: { branches: [ main, "users/**", "feat/**" ] }
  workflow_dispatch:

# Security: Explicitly set minimal permissions
permissions:
  contents: read

jobs:
  sfs-diagnose:
    name: sfs-diagnose
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: |
          du -sh . || true
          git status || true
          echo "diagnose ok"
